{
  "name": "data-agent-question-page",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "tokens:build": "node scripts/build-tokens.mjs",
    "dev": "npm run tokens:build && vite",
    "build": "npm run tokens:build && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^18.0.4",
    "ogl": "^1.0.11",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^6.0.7"
  }
}
