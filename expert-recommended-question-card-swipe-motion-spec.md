# 专家团推荐问题卡片滑动动效说明

> 文档状态：研发交付稿  
> 适用范围：专家团 → 专家详情页 → 推荐问题卡片  
> 生效端：移动端，视口宽度 `≤ 599px`  
> 当前实现：`src/pages/QuestionPage.jsx` + `src/pages/QuestionPage.css`

## 1. 整体效果总结

推荐问题以三层可见的横向叠卡展示：当前卡位于中心并保持完整尺寸，前后卡片从两侧露出，通过水平位移、缩放和轻微旋转形成纸牌式层级。用户横向拖动时，中心卡实时跟手移动，同时逐步缩小并向拖动方向倾斜，后续卡片同步补位；松手后根据拖动距离或甩动速度判定切换，达到阈值则切至相邻卡，未达到则回弹复位。动效优先表现“可拖拽、有层级、可预期”，同时保留卡片内的纵向问题滚动、问题点击与页面滚动能力。

## 2. 交互模型

### 2.1 静止状态

- 视觉上最多展示 3 张卡：当前卡和距离它最近的 2 张卡。
- 中间位置通常是“前一张 + 当前卡 + 后一张”；在首尾位置，其他可见卡可位于同一侧。当总卡数不足 3 张时按实际数量展示，不做循环轮播。
- 只有中心卡可点击和获得焦点；侧后方卡片仅用于层级预览。
- 中心卡内的问题列表可纵向滚动，单条问题最多显示 2 行。

### 2.2 拖动状态

- 按下后不立即锁定方向，位移达到 `6px` 后判定手势轴。
- 水平位移大于纵向位移时，进入横向拖卡；否则交给卡内列表或页面做纵向滚动。
- 拖动期间卡片取消 transition，按每帧指针位置实时跟手。
- 中心卡的位移与手指位移保持 `1:1`，同时最多缩小 `18%`、旋转 `±13°`。
- 其他卡片根据拖动进度在相邻层级姿态之间线性插值，形成连续补位。
- 横向拖动最大距离限制为单次切卡距离的 `1.12 倍`，防止卡片被无限拖离。

### 2.3 松手判定

满足以下任一条件时，切换 1 张相邻卡片：

- 水平拖动距离 `≥ 32px`；
- 水平瞬时速度 `≥ 0.35px/ms`（约 `350px/s`）。

未达到阈值时，当前卡回弹至中心。向左滑进入下一张，向右滑返回上一张。即使快速甩动，单次也只切换 1 张，以避免跳过推荐问题分类。

### 2.4 切换与回弹

- 位移、缩放、旋转：`180ms cubic-bezier(0.2, 0.8, 0.2, 1)`。
- 透明度：`120ms cubic-bezier(0.2, 0.8, 0.2, 1)`。
- 阴影：`280ms cubic-bezier(0.25, 0.46, 0.45, 0.94)`。
- 切换后，新的中心卡成为唯一可交互卡片。

## 3. 卡片姿态参数

基准卡宽为 `258px`；单次切卡距离为实际卡宽的 `52%`，在 `258px` 宽时约为 `134px`。卡高由实际内容和可用空间动态测量，缩放比例需据实际卡高计算。

| 相对层级 | 水平位移 | 视觉高度 | 旋转 | 透明度 | 交互 |
| --- | ---: | ---: | ---: | ---: | --- |
| 中心卡 `0` | `0px` | `100%` | `0°` | `1` | 可点击、可聚焦 |
| 前/后一层 `±1` | `±32px` | 实际卡高 `-30px` | `±3°` | `1` | 不可交互 |
| 前/后二层 `±2` | `±58px` | 实际卡高 `-60px` | `±6°` | `1` | 不可交互 |
| 离场层 `±3` | `±128px` | 实际卡高 `-60px` | `±9°` | `0` | 不可交互 |

缩放计算：

```text
一层缩放比 = (卡高 - 30) / 卡高
二层缩放比 = (卡高 - 60) / 卡高
```

当卡高为 `318px` 时，一层缩放约为 `0.906`，二层缩放约为 `0.811`。所有卡片以中心点为变换原点，层级越靠近中心，`z-index` 越高。

## 4. 手势与状态逻辑

```text
Idle
  ↓ pointer down
PendingAxis
  ├─ 位移 < 6px → 继续等待
  ├─ |Δy| ≥ |Δx| → VerticalScroll（不接管手势）
  └─ |Δx| > |Δy| → HorizontalDrag
       ├─ pointer move → 实时更新全组卡片姿态
       └─ pointer up/cancel
            ├─ 距离/速度达标 → CommitAdjacentCard
            └─ 未达标或 cancel → RestoreCurrentCard
```

建议用 `requestAnimationFrame` 合并高频 `pointermove` 更新，避免在同一帧重复写入样式。速度采用平滑后的瞬时速度：

```text
velocity = previousVelocity × 0.25 + instantVelocity × 0.75
```

## 5. 点击、滚动与边界处理

- 手指位移达到 `6px` 后抑制本次 click，避免拖卡结束后误触推荐问题。
- 点击中心卡中的问题，将该问题回填到当前专家输入框，不直接发送。
- 卡片内容区保留 `pan-y` 和惯性滚动；卡组容器使用 `touch-action: pan-y pinch-zoom`。
- 在第一张继续向右滑，或在最后一张继续向左滑时，不切换索引，松手后回弹复位。
- 不循环、不自动播放、不显示分页圆点。

## 6. 无障碍与减弱动效

- 卡组容器可获得键盘焦点，支持 `ArrowLeft` 和 `ArrowRight` 切换上/下一张。
- 只有中心卡向辅助技术暴露；非中心卡设置 `aria-hidden="true"`，其内问题设置 `tabIndex="-1"`。
- 问题按钮的键盘焦点需保留可见轮廓。
- 在 `prefers-reduced-motion: reduce` 下取消卡片 transition，状态直接切换，不做位移缓动。

## 7. 研发实现要点

1. 按当前索引生成最多 3 张可见卡，为每张卡计算 `relativeIndex = cardIndex - activeIndex`。
2. 将水平位移、纵向位移、缩放、旋转、透明度和层级通过 CSS 变量下发，使拖动更新只改变组合变换参数。
3. 拖动进度为 `dragX / (cardWidth × 0.52)`，非中心卡根据 `relativeIndex + dragProgress` 在相邻姿态之间插值。
4. 拖动时取消 transition；松手后先更新活动索引，再在下一帧清零拖动偏移，由 CSS transition 完成补位或回弹。
5. 卡高使用隐藏测量卡 + `ResizeObserver` 动态同步，避免不同问题数量导致叠卡高度或缩放错位。
6. 变换属性使用 `translate3d/translateZ(0)`、`backface-visibility: hidden` 和适度的 `will-change`，并在性能较差设备上确保拖动期间无明显掉帧。

## 8. 验收标准

- [ ] 移动端进入专家详情页后，当前卡居中，侧后方卡片的位移、缩放、旋转和层级正确。
- [ ] 横向拖动时实时跟手，不出现延迟、闪烁、突跳或文字抖动。
- [ ] 拖动超过 `32px` 可稳定切换；短距离快速甩动可通过速度阈值切换。
- [ ] 未达阈值、手势取消或超出首尾边界时，卡片平滑回弹。
- [ ] 单次手势最多切换 1 张，首尾不循环。
- [ ] 纵向拖动可正常滚动卡内问题或页面，不会误触发横向切卡。
- [ ] 拖动后不误触问题；正常点击问题可回填输入框。
- [ ] 键盘左右方向键可切换卡片，非当前卡不进入 Tab 顺序。
- [ ] 开启系统“减少动效”后，卡片状态可正确切换且无缓动。
- [ ] 在 iOS Safari 与 Android Chrome 上，页面滚动、卡内滚动、横向拖卡三者无明显冲突。

## 9. 非移动端说明

当视口宽度 `> 599px` 时，不展示此叠卡轮播，继续使用专家详情页原有的 Tab 切换和问题列表。本文档中的手势阈值、卡片姿态和回弹动画不作用于桌面端。
